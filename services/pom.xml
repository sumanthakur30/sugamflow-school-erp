<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.sugamflow.school</groupId>
  <artifactId>school-services</artifactId>
  <version>0.1.0-SNAPSHOT</version>
  <packaging>pom</packaging>
  <name>SugamFlow School Services</name>
  <description>
    School domain microservices. Platform infra (gateway, Eureka, Spring Cloud Config, notification delivery)
    lives in D:/sugamflow and is reused — do not duplicate those here.
  </description>

  <modules>
    <module>school-common</module>
    <module>school-settings-service</module>
    <module>subscription-service</module>
    <module>form-builder-service</module>
    <module>workflow-service</module>
    <module>rule-engine-service</module>
    <module>report-builder-service</module>
    <module>school-notification-config-service</module>
    <module>audit-service</module>
    <module>admission-service</module>
    <module>fee-service</module>
    <module>student-service</module>
    <module>attendance-service</module>
    <module>exam-service</module>
    <module>library-service</module>
    <module>hostel-service</module>
    <module>transport-service</module>
    <module>payroll-service</module>
    <module>staff-service</module>
    <module>academic-structure-service</module>
    <module>website-service</module>
    <module>cms-service</module>
    <module>support-service</module>
    <module>compliance-service</module>
  </modules>

  <properties>
    <java.version>17</java.version>
    <spring-boot.version>3.3.5</spring-boot.version>
    <spring-cloud.version>2023.0.3</spring-cloud.version>
    <maven.compiler.release>17</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-dependencies</artifactId>
        <version>${spring-boot.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>org.springframework.cloud</groupId>
        <artifactId>spring-cloud-dependencies</artifactId>
        <version>${spring-cloud.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>com.sugamflow.school</groupId>
        <artifactId>school-common</artifactId>
        <version>${project.version}</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <!-- All modules get JUnit 5 + Mockito + spring-test for free. -->
  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <!-- Operational readiness: Prometheus scrape endpoint on every service. -->
    <dependency>
      <groupId>io.micrometer</groupId>
      <artifactId>micrometer-registry-prometheus</artifactId>
    </dependency>
  </dependencies>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.springframework.boot</groupId>
          <artifactId>spring-boot-maven-plugin</artifactId>
          <version>${spring-boot.version}</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>3.13.0</version>
          <configuration>
            <release>${maven.compiler.release}</release>
            <parameters>true</parameters>
          </configuration>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>
</project>
