import { Component } from '@angular/core';

/** Placeholder for the `**` route; navigation is decided by unknownRouteGuard. */
@Component({
  selector: 'sf-unknown-route',
  standalone: true,
  template: '',
})
export class UnknownRouteComponent {}
